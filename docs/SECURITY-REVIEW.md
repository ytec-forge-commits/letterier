# セキュリティ確認記録 — 2026-09-06

Codex Security Standardのソース監査を完了しました。scan ID: `42bb7135-906a-48fe-ad04-526e31548374`。固定したディレクトリスナップショットに対する監査で、実行時・ビルド・テストの自作100ファイルを確認しました。静的資産、ロックファイル、フォント、第三者の圧縮済みcodec等285ファイルは全文解析対象から除外しており、全資産への完全なセキュリティ保証とは扱いません。

## 報告された2件と対応

|指摘|深刻度|修正|検証|
|---|---|---|---|
|履歴付きバックアップの重複文書と入れ子ZIPで処理量を膨らませられる|LOW|履歴ID/ハッシュ重複を先に拒否。外側と内側のZIP解凍量を共通512MiB予算で数え、すべて検証してから保存|重複2000件を格納前に拒否・共有予算の境界を合成データで確認|
|小さい圧縮画像から過大な画素処理を誘発できる|LOW|画像ヘッダー・形式を検査し、文書全体8000万画素。SVG本体と埋込画像も合算。アニメーションキャッシュを拒否|巨大寸法ヘッダー・累積画素・埋込SVG画像・APNG/WebPキャッシュを小さい合成入力で確認|

該当finding IDs: `csf_e95b02d90fc67fc4e3227746`、`csf_8654bc4610bb6cf14b6b4822`。元スキャンは修正前の証拠として保持し、修正後に監査済みスナップショットを書き換えてはいません。回帰テストは `tests/import-budgets.test.ts`。元指摘はローカルファイルを利用者が開く際の可用性リスクで、ネットワーク侵入や任意ファイルへの書き込みが実証されたものではありません。

別の独立リリースレビューでは、固定文字箱と本文削除の組合せによる保存失敗、および最近使った手紙の読込順による直前編集の巻き戻りを確認し、修正しました。固定要素の用紙位置を変えずアンカーを本文長の範囲へ収め、現在の文書を保存してから回復データを読み込みます。

## 境界

文書・SVGは許可した構造と値に限定し、外部参照や実行内容を拒否します。画像変換はローカルで行います。ネイティブ保存は選択したファイルに限定した保存権限と変更検出、同一ディレクトリ内の一時書込みによる置換を使います。回復データの読み書きはアプリ専用領域へ制限します。

WebView2、Windowsの画像codec、プリンタードライバーは信頼する実行基盤であり、このソース監査でそれらのバイナリを検証したとは扱いません。運用するOSとRuntimeの更新は別途必要です。
