# レタリエ コード署名方針

レタリエのY-TEC Forge向け直接配布版は、WindowsのCurrentUser証明書ストアで保護された、秘密鍵をエクスポートできないY-TEC自己署名コード署名証明書でAuthenticode署名します。実行ファイルとインストーラーにはRFC 3161タイムスタンプを付け、署名検証後に最終ZIPを作成し、公開ファイルごとのSHA-256を提供します。

自己署名は、公開証明書との一致と署名後の改変検知を補助するものです。商用認証局による身元保証ではなく、WindowsやSmartScreenの警告が表示されないことを保証しません。レタリエは証明書を利用者の信頼済み証明書ストアへ自動登録しません。

秘密鍵、PFX、パスワードはWorkspace、Git、配布ZIP、GitHub、Forgeへ保存しません。公開する `.cer` は公開鍵だけを含みます。Microsoft Store版を将来提出する場合はStoreの署名経路を使い、この自己署名証明書を提出物へ強制しません。

配布工程は `build → test → sign → verify → package → SHA-256 → publish` の順とし、署名後にバイナリを変更した場合は署名・検証・ハッシュ生成をやり直します。
